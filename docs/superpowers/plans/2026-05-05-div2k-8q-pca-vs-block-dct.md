# DIV2K-8q PCA-vs-block-DCT Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the DIV2K-8q analog of the QuickDraw PCA-vs-block-DCT experiment — code, run, figures, table, writeup — landing on `main` via PR.

**Architecture:** Five phases on `feat/div2k-8q-pca-vs-block-dct` (branched off `main`). Phase A scaffolds five tools + the entry point. Phase B trains 7 bases across two GPUs in parallel, then cellifies into `by_basis/`. Phase C runs the independent rerun, regenerates figures, and renders the paper table. Phase D clones the QuickDraw writeup, retargets, compiles. Phase E writes the README delta and opens the PR.

**Tech Stack:** Python 3, JAX, `pdft_benchmarks` library, Typst 0.13.

**Spec:** `docs/superpowers/specs/2026-05-05-div2k-8q-pca-vs-block-dct-design.md` on `local/reorg-docs` (commits `fdc34be` + `2c84c7d`). The spec is **local-only**; the engineer reads it from that branch but does not commit it on `feat/div2k-8q-pca-vs-block-dct`. This plan is **also local-only** (saved on `local/reorg-docs`, never pushed, never on `main`).

**Critical contracts:**
- `os.environ["CUDA_VISIBLE_DEVICES"]` MUST be set BEFORE any `pdft_benchmarks` import in the entry point. JAX preallocates ~75% of every visible GPU on first call; without this, two parallel invocations OOM.
- `cellify_run.py` operates on JSON only — must NOT import `pdft_benchmarks` (no GPU).
- Baselines list MUST include `pca` and `block_pca_8`.
- Block bases at m=8 produce 16×16 blocks (not 8×8). Note in writeup; do not override.
- `_runs/` deleted after cellify confirms parity (Stage B3).

---

## File Structure

### Files created on `feat/div2k-8q-pca-vs-block-dct`
- `experiments/div2k_8q_pca_vs_block_dct.py` — REPLACE the placeholder stub with a real entry point.
- `tools/cellify_run.py` — flat `run_experiment` output → `by_basis/<basis>/` tree. Pure JSON; no GPU.
- `tools/independent_div2k_8q_baselines.py` — DIV2K analog of `independent_quickdraw_baselines.py`.
- `tools/render_div2k_paper_table.py` — DIV2K-specific paper table renderer.

### Files modified
- `tools/render_freq_recon_grid.py` — add `--dataset {quickdraw,div2k_8q}` flag.
- `tools/render_pca_basis_visualization.py` — add `--dataset {quickdraw,div2k_8q}` flag.
- `README.md` — flip DIV2K-8q from placeholder to "implemented".
- `results/div2k_8q_pca_vs_block_dct/README.md` — drop the "placeholder" framing.

### Files generated by running the code (committed in Phase D/E)
- `results/div2k_8q_pca_vs_block_dct/by_basis/<basis>/{metrics.json, env.json, trained_<basis>.json, loss_history/<basis>_loss.json}` for each of `qft, entangled_qft, tebd, mera, blocked, rich, real_rich`. Plus `<baseline>_eigenbasis.npz` for PCA-class baselines.
- `results/div2k_8q_pca_vs_block_dct/independent_reruns/seed_default/{metrics.json, REPORT.md}`.
- `results/div2k_8q_pca_vs_block_dct/figures/{ar1_examples.png, freq_recon_grid_img*.png, pca_basis.png}`.
- `results/div2k_8q_pca_vs_block_dct/tables/published_8q_div2k.tex`.
- `results/div2k_8q_pca_vs_block_dct/writeup.{typ,pdf}`.

### Files NOT touched
- `src/pdft_benchmarks/` library code.
- The QuickDraw experiment, its writeup, or its tools' core behavior.

---

## Pre-flight

- [ ] **Step P1: Verify on `local/reorg-docs`, no dirty state**

Run:
```bash
cd /home/claude-user/pdft-benchmarks
git status --short
git branch --show-current
```

Expected: branch is `local/reorg-docs`, status clean.

- [ ] **Step P2: Read both spec docs into your working memory**

Run:
```bash
ls docs/superpowers/specs/2026-05-05-div2k-8q-pca-vs-block-dct-design.md
ls docs/superpowers/plans/2026-05-05-div2k-8q-pca-vs-block-dct.md
```

Expected: both files exist on this branch. The plan you are reading is the second file.

- [ ] **Step P3: Confirm typst is available**

Run: `typst --version`
Expected: `typst 0.13.x` or newer.

- [ ] **Step P4: Confirm GitHub CLI is authenticated**

Run: `gh auth status`
Expected: logged in, with repo scope.

- [ ] **Step P5: Confirm DIV2K dataset is available**

Run:
```bash
ls /home/claude-user/ParametricDFT-Benchmarks.jl/data/DIV2K_train_HR/ | head -3
```

Expected: at least 3 PNG filenames listed. If absent, stop — the training stage will fail.

---

## Task 0: Cut the implementation branch

**Files:** none (branch ref only).

- [ ] **Step 0.1: Sync local main**

Run:
```bash
git checkout main
git fetch origin main
git pull --ff-only origin main
git rev-parse HEAD origin/main
```

Expected: both SHAs match.

- [ ] **Step 0.2: Branch and switch**

Run:
```bash
git checkout -b feat/div2k-8q-pca-vs-block-dct
git branch --show-current
```

Expected: `feat/div2k-8q-pca-vs-block-dct`.

- [ ] **Step 0.3: Verify spec/plan are NOT on this branch**

Run:
```bash
ls docs/superpowers/specs/2026-05-05-div2k-8q-pca-vs-block-dct-design.md 2>&1
ls docs/superpowers/plans/2026-05-05-div2k-8q-pca-vs-block-dct.md 2>&1
```

Expected: both report `No such file or directory`. They live only on `local/reorg-docs`.

To re-read the spec at any time during execution: `git show local/reorg-docs:docs/superpowers/specs/2026-05-05-div2k-8q-pca-vs-block-dct-design.md | less`.

---

## Phase A — Code scaffolding

### Task A1: Replace `experiments/div2k_8q_pca_vs_block_dct.py` with the real entry point

**Files:**
- Modify: `experiments/div2k_8q_pca_vs_block_dct.py` (currently a placeholder stub)

The entry point must (1) parse `--gpu N` and set `CUDA_VISIBLE_DEVICES` before importing `pdft_benchmarks`, (2) parse `--bases` as a comma-separated subset of the 7 trainable bases, (3) call `run_experiment` with the right baselines list including PCA + block-PCA.

- [ ] **Step A1.1: Read the QuickDraw entry point as the template**

Run:
```bash
cat experiments/quickdraw_pca_vs_block_dct.py
```

Note the structure: argparse → run_experiment → print. The QuickDraw version does NOT set `CUDA_VISIBLE_DEVICES` — DIV2K must.

- [ ] **Step A1.2: Replace the placeholder file**

Open `experiments/div2k_8q_pca_vs_block_dct.py`. Replace its entire contents with:

```python
#!/usr/bin/env python3
"""DIV2K-8q PCA-vs-block-DCT benchmark (m=n=8, 256×256 grayscale).

Trains the seven registered parametric bases (qft, entangled_qft, tebd,
mera, blocked, rich, real_rich) and evaluates each against six classical
baselines: global FFT, global DCT, 8×8-block FFT, 8×8-block DCT, global
PCA, and 8×8-block PCA. Includes PCA + block-PCA in the trained run so
the writeup's headline numbers come from a single source.

MERA actually runs at this geometry (m+n=16 = 2^4, unlike QuickDraw
where m+n=10 silently skips MERA). Block bases (blocked, rich,
real_rich) train at m=8 with the symmetric `_blocked` split
(inner_m=4, block_log_m=4), giving 16×16 grids of 16×16 blocks on
256×256 images. Note: classical block_dct_8 uses 8×8 blocks — the
trained block bases (16×16) and classical block-DCT (8×8) are NOT at
the same block scale; this mirrors the QuickDraw pattern (4×4 trained
vs 8×8 classical).

GPU isolation: the --gpu flag sets CUDA_VISIBLE_DEVICES BEFORE
importing pdft_benchmarks (which transitively imports JAX). JAX
preallocates ~75% of every visible GPU on first call; without this
guard, two parallel invocations OOM.

Outputs land in `--out` directly. To use both GPUs:

    # Terminal A — GPU 0 — 4 unblocked bases
    python experiments/div2k_8q_pca_vs_block_dct.py \\
        --gpu 0 --bases qft,entangled_qft,tebd,mera \\
        --out results/div2k_8q_pca_vs_block_dct/_runs/unblocked

    # Terminal B — GPU 1 — 3 block-wrapped bases
    python experiments/div2k_8q_pca_vs_block_dct.py \\
        --gpu 1 --bases blocked,rich,real_rich \\
        --out results/div2k_8q_pca_vs_block_dct/_runs/blocked

After both finish, cellify with `tools/cellify_run.py` to assemble the
canonical `by_basis/<basis>/` tree.
"""

import argparse
import os
import sys

ALL_BASES = ("qft", "entangled_qft", "tebd", "mera",
             "blocked", "rich", "real_rich")
BASELINES = ("fft", "dct", "block_fft_8", "block_dct_8", "pca", "block_pca_8")


def main() -> int:
    parser = argparse.ArgumentParser(
        description="DIV2K-8q PCA-vs-block-DCT benchmark",
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog=__doc__,
    )
    parser.add_argument("--preset", default="generalized",
                        choices=["smoke", "moderate", "generalized"])
    parser.add_argument("--gpu", type=int, default=None,
                        help="GPU index. Sets CUDA_VISIBLE_DEVICES before JAX import.")
    parser.add_argument("--out", default=None,
                        help="Output directory (None → timestamped default).")
    parser.add_argument("--bases", default=",".join(ALL_BASES),
                        help=f"Comma-separated subset of {ALL_BASES}.")
    args = parser.parse_args()

    # CRITICAL: set CUDA_VISIBLE_DEVICES BEFORE importing pdft_benchmarks.
    if args.gpu is not None:
        os.environ["CUDA_VISIBLE_DEVICES"] = str(args.gpu)

    bases = [b.strip() for b in args.bases.split(",") if b.strip()]
    unknown = [b for b in bases if b not in ALL_BASES]
    if unknown:
        print(f"unknown basis name(s): {unknown}; "
              f"choices: {sorted(ALL_BASES)}", file=sys.stderr)
        return 2

    # Imports below trigger JAX device discovery; must come AFTER the env var.
    from pdft_benchmarks.pipeline import run_experiment

    res = run_experiment(
        dataset="div2k",
        m=8, n=8,
        bases=bases,
        baselines=list(BASELINES),
        preset=args.preset,
        output_dir=args.out,
        # When CUDA_VISIBLE_DEVICES already isolated GPU N, the JAX-visible
        # device is index 0 inside this process. Pass "auto" so JAX picks it.
        device="auto",
    )
    print(f"\nDone. Results: {res.output_dir}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step A1.3: Verify the file parses**

Run:
```bash
python -c "import ast, pathlib; ast.parse(pathlib.Path('experiments/div2k_8q_pca_vs_block_dct.py').read_text())" && echo "parse OK"
```

Expected: `parse OK`.

- [ ] **Step A1.4: Verify CUDA_VISIBLE_DEVICES gating works (--help with no GPU set)**

Run:
```bash
python experiments/div2k_8q_pca_vs_block_dct.py --help 2>&1 | head -20
```

Expected: argparse help text printed; no CUDA / JAX errors. (`--help` short-circuits before the JAX import.)

- [ ] **Step A1.5: Verify `--bases` validation rejects unknown names**

Run:
```bash
python experiments/div2k_8q_pca_vs_block_dct.py --bases foo,bar 2>&1; echo "exit=$?"
```

Expected: stderr message `unknown basis name(s): ['foo', 'bar']; choices: ['blocked', ..., 'tebd']` and `exit=2`.

- [ ] **Step A1.6: Commit**

Run:
```bash
git add experiments/div2k_8q_pca_vs_block_dct.py
git commit -m "$(cat <<'EOF'
feat(div2k-8q): real entry point for DIV2K-8q PCA-vs-block-DCT

Replaces the placeholder stub with a runnable entry point that:
- Sets CUDA_VISIBLE_DEVICES before importing pdft_benchmarks (so two
  parallel invocations on different GPUs do not collide on JAX's
  default ~75% memory preallocation).
- Accepts --bases as a comma-separated subset of the 7 trainable bases
  so two terminals can split work across two GPUs.
- Includes pca + block_pca_8 in the baselines list so headline numbers
  come from the trained run, not the independent rerun.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task A2: Create `tools/cellify_run.py`

**Files:**
- Create: `tools/cellify_run.py`

This is a pure-Python JSON-shuffler. It must NOT import `pdft_benchmarks` (no GPU dependency).

- [ ] **Step A2.1: Create the file**

Create `tools/cellify_run.py` with:

```python
#!/usr/bin/env python3
"""Split a flat `run_experiment` output dir into a by_basis/<basis>/ tree.

`pdft_benchmarks.pipeline.run_experiment` writes a flat output dir:

    <in>/metrics.json                # top-level dict keyed by basis name
    <in>/env.json
    <in>/trained_<basis>.json        # one per basis
    <in>/loss_history/<basis>_loss.json
    <in>/<baseline>_eigenbasis.npz   # for PCA-class baselines

This tool produces, per basis listed in `metrics.json`:

    <out>/<basis>/metrics.json       # the aggregate's [basis] subtree
    <out>/<basis>/env.json           # copy of the shared run env
    <out>/<basis>/trained_<basis>.json   # move
    <out>/<basis>/loss_history/<basis>_loss.json   # move
    <out>/<basis>/<baseline>_eigenbasis.npz        # copy (shared across cells)

Pure-Python: no JAX, no pdft_benchmarks import — runs without a GPU.

CLI:
    python tools/cellify_run.py --in <flat-dir> --out <by_basis-dst>
                                [--bases comma,sep] [--keep-source]
"""
from __future__ import annotations

import argparse
import json
import shutil
import sys
from pathlib import Path


def cellify(in_dir: Path, out_dir: Path, bases: list[str] | None,
            keep_source: bool) -> int:
    metrics_path = in_dir / "metrics.json"
    env_path = in_dir / "env.json"

    if not metrics_path.exists():
        print(f"[cellify] FAIL: {metrics_path} not found", file=sys.stderr)
        return 1
    if not env_path.exists():
        print(f"[cellify] FAIL: {env_path} not found", file=sys.stderr)
        return 1

    metrics = json.loads(metrics_path.read_text())
    env = env_path.read_text()  # copied verbatim into each cell

    target_bases = bases if bases else list(metrics.keys())
    missing = [b for b in target_bases if b not in metrics]
    if missing:
        print(f"[cellify] FAIL: bases not in metrics.json: {missing}",
              file=sys.stderr)
        return 1

    # Eigenbasis files (shared across cells; copied so cells are self-contained)
    eigenbasis_files = list(in_dir.glob("*_eigenbasis.npz"))

    out_dir.mkdir(parents=True, exist_ok=True)
    for basis in target_bases:
        cell = out_dir / basis
        cell.mkdir(parents=True, exist_ok=True)

        # 1. metrics.json — single-basis subset
        (cell / "metrics.json").write_text(
            json.dumps({basis: metrics[basis]}, indent=2)
        )

        # 2. env.json — copy
        (cell / "env.json").write_text(env)

        # 3. trained_<basis>.json — move (or copy if --keep-source)
        trained = in_dir / f"trained_{basis}.json"
        if trained.exists():
            target = cell / f"trained_{basis}.json"
            if keep_source:
                shutil.copy2(trained, target)
            else:
                shutil.move(str(trained), str(target))

        # 4. loss_history/<basis>_loss.json — move
        loss = in_dir / "loss_history" / f"{basis}_loss.json"
        if loss.exists():
            (cell / "loss_history").mkdir(parents=True, exist_ok=True)
            target = cell / "loss_history" / f"{basis}_loss.json"
            if keep_source:
                shutil.copy2(loss, target)
            else:
                shutil.move(str(loss), str(target))

        # 5. eigenbasis files — copy (shared across cells)
        for eb in eigenbasis_files:
            shutil.copy2(eb, cell / eb.name)

        print(f"[cellify] wrote {cell}/")

    return 0


def main() -> int:
    ap = argparse.ArgumentParser(description=__doc__,
                                 formatter_class=argparse.RawDescriptionHelpFormatter)
    ap.add_argument("--in", dest="in_dir", required=True, type=Path,
                    help="Flat run_experiment output dir.")
    ap.add_argument("--out", required=True, type=Path,
                    help="Destination by_basis/ root.")
    ap.add_argument("--bases", default=None,
                    help="Optional comma-separated subset to cellify.")
    ap.add_argument("--keep-source", action="store_true",
                    help="Copy instead of move; leaves <in> intact.")
    args = ap.parse_args()
    bases = [b.strip() for b in args.bases.split(",")] if args.bases else None
    return cellify(args.in_dir, args.out, bases, args.keep_source)


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step A2.2: Verify it parses and `--help` works**

Run:
```bash
python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/cellify_run.py').read_text())" && echo "parse OK"
python tools/cellify_run.py --help | head -5
```

Expected: `parse OK`; argparse help.

- [ ] **Step A2.3: Verify it does NOT import pdft_benchmarks**

Run:
```bash
grep -n "pdft_benchmarks\|import jax" tools/cellify_run.py
```

Expected: no matches. Cellify must be a pure-Python tool.

- [ ] **Step A2.4: Quick functional smoke test against an existing flat run**

The QuickDraw cells under `results/quickdraw_pca_vs_block_dct/by_basis/` are already cellified, so we don't have a flat run on disk. Synthesize a minimal fake flat run and cellify it:

```bash
mkdir -p /tmp/cellify_test/in/loss_history
echo '{"qft": {"hello": "world"}, "tebd": {"hello": "earth"}}' > /tmp/cellify_test/in/metrics.json
echo '{"env": "test"}' > /tmp/cellify_test/in/env.json
echo '"qft trained"' > /tmp/cellify_test/in/trained_qft.json
echo '"tebd trained"' > /tmp/cellify_test/in/trained_tebd.json
echo '[]' > /tmp/cellify_test/in/loss_history/qft_loss.json
echo '[]' > /tmp/cellify_test/in/loss_history/tebd_loss.json

python tools/cellify_run.py --in /tmp/cellify_test/in --out /tmp/cellify_test/out --keep-source

ls /tmp/cellify_test/out/qft/ /tmp/cellify_test/out/tebd/
cat /tmp/cellify_test/out/qft/metrics.json
```

Expected: each cell dir contains `metrics.json`, `env.json`, `trained_<basis>.json`, `loss_history/<basis>_loss.json`. `qft/metrics.json` is `{"qft": {"hello": "world"}}` (single-basis).

Cleanup: `rm -rf /tmp/cellify_test`.

- [ ] **Step A2.5: Commit**

Run:
```bash
git add tools/cellify_run.py
git commit -m "$(cat <<'EOF'
feat(tools): cellify_run.py — flat run output → by_basis/ tree

Replaces the deleted extract_canonical_cells.py with a simpler shape
aligned to the new by_basis/ layout. Pure-Python (no JAX, no
pdft_benchmarks import) so it runs without a GPU.

Per-basis cell layout: metrics.json (single-basis subset of the
aggregate), env.json (copy), trained_<basis>.json (move), loss_history/
<basis>_loss.json (move), <baseline>_eigenbasis.npz (copy — shared
across cells).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task A3: Create `tools/independent_div2k_8q_baselines.py`

**Files:**
- Create: `tools/independent_div2k_8q_baselines.py`

This is a near-clone of `tools/independent_quickdraw_baselines.py` with the dataset loader swapped.

- [ ] **Step A3.1: Read the QuickDraw version end-to-end**

Run:
```bash
cat tools/independent_quickdraw_baselines.py
```

Note the structure: argparse → set CUDA_VISIBLE_DEVICES → import → load_quickdraw → loop over BASELINE_FACTORIES → write metrics.json + REPORT.md.

- [ ] **Step A3.2: Create the DIV2K version**

Create `tools/independent_div2k_8q_baselines.py` with:

```python
#!/usr/bin/env python3
"""Independent rerun of the DIV2K-8q baselines (n_train=500, m=n=8).

Runs every registered baseline — including PCA, block_PCA_8, and the
classical FFT/DCT family — against a fresh load of the DIV2K-8q test
set, with no trained bases. Produces a single metrics.json + a
Markdown report so the numbers in
results/div2k_8q_pca_vs_block_dct/writeup.typ can be verified.

Sets CUDA_VISIBLE_DEVICES before importing pdft_benchmarks (matches
the entry-point pattern); independent rerun is single-process so this
is mostly defensive.
"""

from __future__ import annotations

import argparse
import json
import os
import time
from pathlib import Path


def main() -> int:
    ap = argparse.ArgumentParser(description=__doc__)
    ap.add_argument("--out",
                    default="results/div2k_8q_pca_vs_block_dct/independent_reruns/seed_default",
                    help="Output directory")
    ap.add_argument("--gpu", type=int, default=0)
    ap.add_argument("--seed", type=int, default=42)
    ap.add_argument("--n-train", type=int, default=500)
    ap.add_argument("--n-test", type=int, default=50)
    ap.add_argument("--img-size", type=int, default=256)
    args = ap.parse_args()

    os.environ["CUDA_VISIBLE_DEVICES"] = str(args.gpu)

    # Imports trigger JAX initialisation; must come AFTER the env var.
    from pdft_benchmarks.baselines import BASELINE_FACTORIES
    from pdft_benchmarks.datasets import load_div2k
    from pdft_benchmarks.evaluation import evaluate_baseline

    out = Path(args.out)
    out.mkdir(parents=True, exist_ok=True)

    print(f"[indep] loading DIV2K n_train={args.n_train}, n_test={args.n_test}, "
          f"img_size={args.img_size}, seed={args.seed}…")
    train, test = load_div2k(
        args.n_train, args.n_test, seed=args.seed, size=args.img_size
    )
    print(f"[indep] train shape={train.shape}, test shape={test.shape}")

    keep_ratios = (0.05, 0.10, 0.15, 0.20)

    metrics: dict = {}
    for name in sorted(BASELINE_FACTORIES):
        print(f"[indep] fitting + evaluating baseline '{name}'…")
        t0 = time.perf_counter()
        builder = BASELINE_FACTORIES[name]
        fn = builder(list(train))
        kr_metrics, elapsed = evaluate_baseline(fn, test, keep_ratios)
        metrics[name] = {"metrics": kr_metrics, "time_s": elapsed}
        print(f"[indep]   done in {elapsed:.1f}s")

    out_metrics = out / "metrics.json"
    out_metrics.write_text(json.dumps(metrics, indent=2))
    print(f"[indep] wrote {out_metrics}")

    # Report
    report = ["# DIV2K-8q independent baseline rerun",
              "",
              f"- seed: {args.seed}",
              f"- n_train: {args.n_train}, n_test: {args.n_test}",
              f"- img_size: {args.img_size}",
              f"- baselines: {sorted(BASELINE_FACTORIES)}",
              "",
              "## PSNR (mean over test set, keep ratios = 0.05/0.10/0.15/0.20)",
              "",
              "| baseline | 0.05 | 0.10 | 0.15 | 0.20 |",
              "|---|---|---|---|---|"]
    for name in sorted(metrics):
        m = metrics[name]["metrics"]
        psnrs = [f"{m[str(k)]['mean_psnr']:.2f}" for k in (0.05, 0.10, 0.15, 0.20)]
        report.append(f"| {name} | {' | '.join(psnrs)} |")
    (out / "REPORT.md").write_text("\n".join(report) + "\n")
    print(f"[indep] wrote {out / 'REPORT.md'}")

    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step A3.3: Verify it parses**

Run:
```bash
python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/independent_div2k_8q_baselines.py').read_text())" && echo "parse OK"
python tools/independent_div2k_8q_baselines.py --help | head -10
```

Expected: `parse OK`; argparse help.

- [ ] **Step A3.4: Commit**

Run:
```bash
git add tools/independent_div2k_8q_baselines.py
git commit -m "$(cat <<'EOF'
feat(tools): independent_div2k_8q_baselines.py — DIV2K verifier

DIV2K analog of independent_quickdraw_baselines.py. Loads DIV2K test
images at 256×256, fits every registered baseline (FFT/DCT/block_FFT/
block_DCT/PCA/block_PCA), writes metrics.json + REPORT.md to the
seed_default rerun dir. Sets CUDA_VISIBLE_DEVICES before importing
pdft_benchmarks per the entry-point pattern.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task A4: Create `tools/render_div2k_paper_table.py`

**Files:**
- Create: `tools/render_div2k_paper_table.py`

Reads each `by_basis/<basis>/metrics.json` and produces a single .tex table in the same column-format as `published_8q_quickdraw_v2.tex`.

- [ ] **Step A4.1: Inspect the existing QuickDraw table for the format to match**

Run:
```bash
cat results/quickdraw_pca_vs_block_dct/tables/published_8q_quickdraw_v2.tex
```

Note: it's a `tabular` block with rows for classical baselines + trained bases, columns for keep ratios 0.05 / 0.10 / 0.15 / 0.20, cells showing PSNR values. The exact column count and grouping pattern is the template.

- [ ] **Step A4.2: Inspect the metrics.json schema**

Run:
```bash
head -25 results/quickdraw_pca_vs_block_dct/by_basis/qft/metrics.json
```

Note: top-level keyed by basis name; inside each basis, `metrics["0.05"]["mean_psnr"]` is the value at keep ratio 0.05, etc. The cell aggregates BOTH the trained basis and the baselines that were evaluated alongside it (each cell has the same baselines, they're just scoped to the basis name keying).

Wait — verify: in the cellified DIV2K layout, each cell's `metrics.json` will have only the trained basis as a top-level key (e.g. `{"qft": {...}}`); the baselines do NOT appear there because `pipeline.py` writes baselines as their own top-level keys in the aggregate, and cellify pulls only the trained-basis subtree per cell. So baseline metrics live in the cells too only if the cellify keeps the baseline keys somewhere.

Re-read cellify behavior: `cellify` pulls `metrics[basis]` for each trained basis. The aggregate also has top-level entries for each baseline (`metrics["fft"]`, `metrics["pca"]`, etc.). Those baseline-keyed entries are NOT pulled into any single cell.

For the paper table, we need both trained-basis numbers AND baseline numbers. Two options:
  - (a) Have the paper table read both the cellified `<basis>/metrics.json` AND a separate baselines source (e.g. one of the cells' env.json provenance, or a single baselines.json that we save alongside).
  - (b) Have cellify also produce `<dst>/_baselines.json` that contains the baseline-keyed entries from the aggregate.
  - (c) Have the paper table read the original flat `_runs/<group>/metrics.json` for baselines (but those are deleted after cellify cleanup — bad).
  - (d) Have cellify duplicate the baseline entries into every cell's metrics.json.

Pick **(b)**: cleanest — one extra small file, no baseline duplication, paper-table reads from a single canonical place.

This means we need to update the cellify spec to also emit `<dst>/_baselines.json`. Go back to Task A2 mentally and add this. Actually we'll handle it here in Task A4 by generating it on the fly: read the flat `_runs/<group>/metrics.json` BEFORE cellify deletes it, OR have the paper-table renderer read both.

Cleanest: amend cellify NOW (re-edit Task A2's file). Step A4.3 patches cellify; Step A4.4 builds the renderer.

- [ ] **Step A4.3: Patch `tools/cellify_run.py` to also emit `_baselines.json`**

Edit `tools/cellify_run.py`. Find the section that builds per-basis cells (the `for basis in target_bases:` loop). Immediately AFTER the loop closes, before `return 0`, add:

```python
    # Baseline-keyed entries from the aggregate metrics.json get saved
    # to <out>/_baselines.json for the paper-table renderer. (Cellify
    # treats every top-level key NOT in target_bases as a baseline
    # key.)
    baseline_entries = {k: v for k, v in metrics.items()
                        if k not in target_bases}
    if baseline_entries:
        baselines_path = out_dir / "_baselines.json"
        # Merge with any pre-existing _baselines.json from a previous
        # cellify of a sibling _runs/<group>/ — different groups train
        # different bases but the baseline numbers should agree across
        # groups (all baselines are evaluated against every group's
        # train split, which is the same seed).
        if baselines_path.exists():
            existing = json.loads(baselines_path.read_text())
            existing.update(baseline_entries)
            baseline_entries = existing
        baselines_path.write_text(json.dumps(baseline_entries, indent=2))
        print(f"[cellify] wrote {baselines_path}")
```

- [ ] **Step A4.4: Re-run the cellify smoke test to verify _baselines.json emission**

Run:
```bash
mkdir -p /tmp/cellify_test/in/loss_history
echo '{"qft": {"hello": "world"}, "tebd": {"hello": "earth"}, "fft": {"baseline": 1}, "pca": {"baseline": 2}}' > /tmp/cellify_test/in/metrics.json
echo '{"env": "test"}' > /tmp/cellify_test/in/env.json
echo '"qft trained"' > /tmp/cellify_test/in/trained_qft.json
echo '"tebd trained"' > /tmp/cellify_test/in/trained_tebd.json
echo '[]' > /tmp/cellify_test/in/loss_history/qft_loss.json
echo '[]' > /tmp/cellify_test/in/loss_history/tebd_loss.json

python tools/cellify_run.py --in /tmp/cellify_test/in --out /tmp/cellify_test/out --bases qft,tebd --keep-source

ls /tmp/cellify_test/out/
cat /tmp/cellify_test/out/_baselines.json
```

Expected: `/tmp/cellify_test/out/` contains `qft/`, `tebd/`, AND `_baselines.json`. The baselines file is `{"fft": {"baseline": 1}, "pca": {"baseline": 2}}`.

Cleanup: `rm -rf /tmp/cellify_test`.

- [ ] **Step A4.5: Amend the cellify commit (or create a new commit)**

The `--bases` flag was already used in the smoke test, so the patch is functional. Stage and commit (new commit, not amend, per the user's "create new commits, don't amend" preference):

```bash
git add tools/cellify_run.py
git commit -m "$(cat <<'EOF'
feat(tools): cellify also writes _baselines.json

Top-level baseline-keyed entries in the aggregate metrics.json (fft,
dct, pca, etc.) are not associated with any single trained basis.
Save them to <out>/_baselines.json so the paper-table renderer has a
canonical source. Cellifying multiple sibling _runs/<group>/ dirs
into the same <out> merges baseline entries safely (cross-group
baseline numbers should agree at the same seed).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step A4.6: Create `tools/render_div2k_paper_table.py`**

Create `tools/render_div2k_paper_table.py` with:

```python
#!/usr/bin/env python3
"""Generate the LaTeX paper table for DIV2K-8q PCA-vs-block-DCT.

Reads:
    <by-basis>/<basis>/metrics.json     # per trained basis
    <by-basis>/_baselines.json          # baseline numbers from cellify

Writes:
    --out (default: results/div2k_8q_pca_vs_block_dct/tables/published_8q_div2k.tex)

Output format mirrors results/quickdraw_pca_vs_block_dct/tables/
published_8q_quickdraw_v2.tex: a tabular block with rows grouped as
(classical-fixed / classical-fitted / learned-unblocked / learned-blocked)
and columns for keep ratios.
"""
from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path

KEEP_RATIOS = (0.05, 0.10, 0.15, 0.20)

# Row order in the paper table. Must match the QuickDraw table's grouping.
ROWS = [
    # (label, key, group)
    ("FFT",                "fft",            "classical"),
    ("DCT",                "dct",            "classical"),
    ("Block-FFT 8",        "block_fft_8",    "classical"),
    ("Block-DCT 8",        "block_dct_8",    "classical"),
    ("PCA",                "pca",            "fitted"),
    ("Block-PCA 8",        "block_pca_8",    "fitted"),
    ("QFT",                "qft",            "learned"),
    ("Entangled QFT",      "entangled_qft",  "learned"),
    ("TEBD",               "tebd",           "learned"),
    ("MERA",               "mera",           "learned"),
    ("Blocked",            "blocked",        "learned-blocked"),
    ("Rich",               "rich",           "learned-blocked"),
    ("Real Rich",          "real_rich",      "learned-blocked"),
]


def fmt_psnr(v: float | None) -> str:
    if v is None:
        return "—"
    return f"{v:.2f}"


def get_psnr(entry: dict, kr: float) -> float | None:
    """Pull mean_psnr at keep ratio kr from a metrics-style entry."""
    if not entry or "metrics" not in entry:
        return None
    m = entry["metrics"]
    # Keys may be "0.05" or "0.1" (Python json normalizes 0.10 → 0.1).
    for k in (f"{kr}", f"{kr:.2f}", f"{kr:.1f}"):
        if k in m:
            return m[k].get("mean_psnr")
    return None


def render(by_basis: Path) -> str:
    # Load baselines
    baselines_path = by_basis / "_baselines.json"
    baselines = json.loads(baselines_path.read_text()) if baselines_path.exists() else {}

    # Load each trained basis cell
    trained: dict[str, dict] = {}
    for basis_dir in sorted(by_basis.iterdir()):
        if not basis_dir.is_dir():
            continue
        cell_metrics = basis_dir / "metrics.json"
        if not cell_metrics.exists():
            continue
        cell = json.loads(cell_metrics.read_text())
        # Each cell has exactly one top-level key — the basis name.
        for k, v in cell.items():
            trained[k] = v

    # Build the LaTeX
    lines = [
        "% Trimmed headline rate-distortion table for the paper.",
        "% Rows: 4 classical-fixed + 2 classical-fitted (PCA) + 4 learned-unblocked + 3 learned-blocked",
        "% Generated by tools/render_div2k_paper_table.py from",
        "% results/div2k_8q_pca_vs_block_dct/by_basis/.",
        "% PSNR (dB), mean over n_test=50 DIV2K test images.",
        "%",
        "\\begin{tabular}{l" + "c" * len(KEEP_RATIOS) + "}",
        "\\toprule",
        "Method & " + " & ".join(f"$\\rho={kr}$" for kr in KEEP_RATIOS) + " \\\\",
        "\\midrule",
    ]
    last_group = None
    for label, key, group in ROWS:
        if last_group is not None and group != last_group:
            lines.append("\\midrule")
        last_group = group
        entry = baselines.get(key) or trained.get(key)
        psnrs = [fmt_psnr(get_psnr(entry, kr)) for kr in KEEP_RATIOS]
        lines.append(f"{label} & " + " & ".join(psnrs) + " \\\\")
    lines.append("\\bottomrule")
    lines.append("\\end{tabular}")
    return "\n".join(lines) + "\n"


def main() -> int:
    ap = argparse.ArgumentParser(description=__doc__,
                                 formatter_class=argparse.RawDescriptionHelpFormatter)
    ap.add_argument("--by-basis", type=Path,
                    default=Path("results/div2k_8q_pca_vs_block_dct/by_basis"))
    ap.add_argument("--out", type=Path,
                    default=Path("results/div2k_8q_pca_vs_block_dct/tables/published_8q_div2k.tex"))
    args = ap.parse_args()

    if not args.by_basis.is_dir():
        print(f"[paper-table] FAIL: --by-basis {args.by_basis} is not a directory",
              file=sys.stderr)
        return 1

    text = render(args.by_basis)
    args.out.parent.mkdir(parents=True, exist_ok=True)
    args.out.write_text(text)
    print(f"[paper-table] wrote {args.out} ({len(text)} bytes)")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step A4.7: Verify it parses and `--help` works**

Run:
```bash
python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/render_div2k_paper_table.py').read_text())" && echo "parse OK"
python tools/render_div2k_paper_table.py --help | head -5
```

Expected: `parse OK`; argparse help.

- [ ] **Step A4.8: Commit**

Run:
```bash
git add tools/render_div2k_paper_table.py
git commit -m "$(cat <<'EOF'
feat(tools): render_div2k_paper_table.py — DIV2K paper table renderer

Reads by_basis/<basis>/metrics.json + _baselines.json and emits a
LaTeX tabular block matching the QuickDraw v2 format. Pure-Python
(no GPU). Does not fix the existing tools/render_paper_table.py
(directory-walk TODO from the reorg) — this is DIV2K-specific.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task A5: Extend `tools/render_freq_recon_grid.py` with `--dataset` flag

**Files:**
- Modify: `tools/render_freq_recon_grid.py`

- [ ] **Step A5.1: Read the current file**

Run: `cat tools/render_freq_recon_grid.py | head -120`

Note the `load_quickdraw` call site, the `results/quickdraw_pca_vs_block_dct/by_basis/{name}/trained_{name}.json` path, and the `--out` default. All three need to branch on `--dataset`.

- [ ] **Step A5.2: Add the `--dataset` argparse and branch the loader + path**

Edit `tools/render_freq_recon_grid.py`:

A) Add at the top of `main()` (right after the existing argparse block, before `os.environ["CUDA_VISIBLE_DEVICES"]`):

Find:
```python
    ap.add_argument("--out", default="results/quickdraw_pca_vs_block_dct/figures/freq_recon_grid.png")
    ap.add_argument("--gpu", type=int, default=0)
    args = ap.parse_args()
```

Replace with:
```python
    ap.add_argument("--out", default=None,
                    help="Output PNG path. None → auto-derived from --dataset.")
    ap.add_argument("--gpu", type=int, default=0)
    ap.add_argument("--dataset", choices=["quickdraw", "div2k_8q"],
                    default="quickdraw",
                    help="Which dataset+experiment-tree to render against.")
    args = ap.parse_args()

    DATASET_CONFIG = {
        "quickdraw": {
            "by_basis": "results/quickdraw_pca_vs_block_dct/by_basis",
            "out_default": "results/quickdraw_pca_vs_block_dct/figures/freq_recon_grid.png",
            "img_size": 32,
        },
        "div2k_8q": {
            "by_basis": "results/div2k_8q_pca_vs_block_dct/by_basis",
            "out_default": "results/div2k_8q_pca_vs_block_dct/figures/freq_recon_grid.png",
            "img_size": 256,
        },
    }
    cfg = DATASET_CONFIG[args.dataset]
    if args.out is None:
        args.out = cfg["out_default"]
```

B) Find the `from pdft_benchmarks.datasets import load_quickdraw` line and the train/test load. Replace:
```python
    from pdft_benchmarks.datasets import load_quickdraw
    ...
    train, test = load_quickdraw(args.n_train, args.n_test, seed=args.seed, img_size=32)
```

With:
```python
    if args.dataset == "quickdraw":
        from pdft_benchmarks.datasets import load_quickdraw
        train, test = load_quickdraw(args.n_train, args.n_test, seed=args.seed, img_size=cfg["img_size"])
    elif args.dataset == "div2k_8q":
        from pdft_benchmarks.datasets import load_div2k
        train, test = load_div2k(args.n_train, args.n_test, seed=args.seed, size=cfg["img_size"])
    else:
        raise ValueError(f"unknown dataset: {args.dataset}")
```

C) Find the `path = Path(f"results/quickdraw_pca_vs_block_dct/by_basis/{name}/trained_{name}.json")` line. Replace with:
```python
        path = Path(cfg["by_basis"]) / name / f"trained_{name}.json"
```

- [ ] **Step A5.3: Verify the file parses and `--help` shows `--dataset`**

Run:
```bash
python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/render_freq_recon_grid.py').read_text())" && echo "parse OK"
python tools/render_freq_recon_grid.py --help 2>&1 | grep -A1 "dataset"
```

Expected: `parse OK`; `--help` includes `--dataset {quickdraw,div2k_8q}`.

- [ ] **Step A5.4: Verify backward compat — no `--dataset` defaults to QuickDraw**

Run:
```bash
python tools/render_freq_recon_grid.py --help 2>&1 | grep "default:"
```

Expected: a line indicating quickdraw is the default.

- [ ] **Step A5.5: Commit**

Run:
```bash
git add tools/render_freq_recon_grid.py
git commit -m "$(cat <<'EOF'
feat(tools): render_freq_recon_grid supports --dataset div2k_8q

Adds --dataset {quickdraw,div2k_8q} flag with quickdraw as default
(backward-compatible). Branches the dataset loader, the trained-basis
path, and the --out default. Used by Phase C of the DIV2K-8q
experiment plan.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task A6: Extend `tools/render_pca_basis_visualization.py` with `--dataset` flag

**Files:**
- Modify: `tools/render_pca_basis_visualization.py`

Same pattern as Task A5.

- [ ] **Step A6.1: Add `--dataset` and the dataset config**

Edit `tools/render_pca_basis_visualization.py`. Find the argparse block:
```python
    ap.add_argument("--out", default="results/quickdraw_pca_vs_block_dct/figures/pca_basis.png")
    ap.add_argument("--gpu", type=int, default=0)
    args = ap.parse_args()
```

Replace with:
```python
    ap.add_argument("--out", default=None,
                    help="Output PNG path. None → auto-derived from --dataset.")
    ap.add_argument("--gpu", type=int, default=0)
    ap.add_argument("--dataset", choices=["quickdraw", "div2k_8q"],
                    default="quickdraw",
                    help="Which dataset to fit PCA against.")
    args = ap.parse_args()

    DATASET_CONFIG = {
        "quickdraw": {
            "out_default": "results/quickdraw_pca_vs_block_dct/figures/pca_basis.png",
            "img_size": 32,
        },
        "div2k_8q": {
            "out_default": "results/div2k_8q_pca_vs_block_dct/figures/pca_basis.png",
            "img_size": 256,
        },
    }
    cfg = DATASET_CONFIG[args.dataset]
    if args.out is None:
        args.out = cfg["out_default"]
```

- [ ] **Step A6.2: Branch the dataset loader**

Find the `load_quickdraw` import and call. Replace:
```python
    from pdft_benchmarks.datasets import load_quickdraw
    ...
    train, _ = load_quickdraw(args.n_train, args.n_test, seed=args.seed, img_size=32)
```

With:
```python
    if args.dataset == "quickdraw":
        from pdft_benchmarks.datasets import load_quickdraw
        train, _ = load_quickdraw(args.n_train, args.n_test, seed=args.seed, img_size=cfg["img_size"])
    elif args.dataset == "div2k_8q":
        from pdft_benchmarks.datasets import load_div2k
        train, _ = load_div2k(args.n_train, args.n_test, seed=args.seed, size=cfg["img_size"])
    else:
        raise ValueError(f"unknown dataset: {args.dataset}")
```

(If the script reads `train` from `load_quickdraw` differently, adapt the unpacking but keep the `--dataset` branch logic.)

- [ ] **Step A6.3: Verify**

Run:
```bash
python -c "import ast, pathlib; ast.parse(pathlib.Path('tools/render_pca_basis_visualization.py').read_text())" && echo "parse OK"
python tools/render_pca_basis_visualization.py --help 2>&1 | grep -A1 "dataset"
```

Expected: `parse OK`; argparse shows `--dataset`.

- [ ] **Step A6.4: Commit**

Run:
```bash
git add tools/render_pca_basis_visualization.py
git commit -m "$(cat <<'EOF'
feat(tools): render_pca_basis_visualization supports --dataset div2k_8q

Same pattern as render_freq_recon_grid: --dataset {quickdraw,div2k_8q}
with quickdraw as default. For DIV2K, global PCA produces 256×256
eigen-images instead of 32×32; block PCA still 8×8 patches.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase B — Run training + cellify

This phase actually executes the training. It takes real GPU-time. The user will run the two terminal-A / terminal-B invocations themselves; the plan documents the exact commands.

### Task B1: Two-GPU parallel training

**Output:**
- `results/div2k_8q_pca_vs_block_dct/_runs/unblocked/{metrics.json, env.json, trained_*.json, loss_history/, *_eigenbasis.npz}`
- `results/div2k_8q_pca_vs_block_dct/_runs/blocked/{...}` (same shape, different bases)

- [ ] **Step B1.1: Start Terminal A — GPU 0 — 4 unblocked bases**

In the first terminal window, run:
```bash
cd /home/claude-user/pdft-benchmarks
python experiments/div2k_8q_pca_vs_block_dct.py \
    --gpu 0 --bases qft,entangled_qft,tebd,mera \
    --out results/div2k_8q_pca_vs_block_dct/_runs/unblocked
```

Expected: training proceeds; per-basis progress logged. Wall-clock: real GPU-hours (depends on hardware). Output appears under the named dir as bases complete.

- [ ] **Step B1.2: Start Terminal B — GPU 1 — 3 block-wrapped bases**

In a second terminal window, run:
```bash
cd /home/claude-user/pdft-benchmarks
python experiments/div2k_8q_pca_vs_block_dct.py \
    --gpu 1 --bases blocked,rich,real_rich \
    --out results/div2k_8q_pca_vs_block_dct/_runs/blocked
```

Expected: same kind of progress.

- [ ] **Step B1.3: Wait for both to finish**

Monitor both terminals. Each should print `Done. Results: <path>` when complete.

If either crashes mid-way: inspect `_runs/<group>/` for which `trained_<basis>.json` files exist, then re-invoke with `--bases` of only the missing ones (the pipeline does NOT auto-resume; it overwrites whatever's there). Example:
```bash
ls results/div2k_8q_pca_vs_block_dct/_runs/unblocked/trained_*.json
# If only qft + entangled_qft completed, re-run:
python experiments/div2k_8q_pca_vs_block_dct.py \
    --gpu 0 --bases tebd,mera \
    --out results/div2k_8q_pca_vs_block_dct/_runs/unblocked
```
Note: re-running with the SAME `--out` will overwrite that dir's `metrics.json` and `env.json` with the new run's, losing the metrics for the previously-completed bases. To avoid this, re-run with a different `--out` and merge.

- [ ] **Step B1.4: Verify both runs completed**

Run:
```bash
ls results/div2k_8q_pca_vs_block_dct/_runs/unblocked/trained_*.json | wc -l
ls results/div2k_8q_pca_vs_block_dct/_runs/blocked/trained_*.json | wc -l
ls results/div2k_8q_pca_vs_block_dct/_runs/unblocked/metrics.json
ls results/div2k_8q_pca_vs_block_dct/_runs/blocked/metrics.json
```

Expected: 4 trained files in unblocked, 3 in blocked, both `metrics.json` files exist.

### Task B2: Cellify both runs

- [ ] **Step B2.1: Cellify unblocked run**

Run:
```bash
python tools/cellify_run.py \
    --in  results/div2k_8q_pca_vs_block_dct/_runs/unblocked \
    --out results/div2k_8q_pca_vs_block_dct/by_basis
```

Expected: prints `[cellify] wrote <path>/qft/`, `entangled_qft/`, `tebd/`, `mera/`, then `[cellify] wrote <out>/_baselines.json`.

- [ ] **Step B2.2: Cellify blocked run**

Run:
```bash
python tools/cellify_run.py \
    --in  results/div2k_8q_pca_vs_block_dct/_runs/blocked \
    --out results/div2k_8q_pca_vs_block_dct/by_basis
```

Expected: `[cellify] wrote <path>/blocked/`, `rich/`, `real_rich/`, then merge of `_baselines.json` (re-written with combined entries).

- [ ] **Step B2.3: Verify the cellified tree shape**

Run:
```bash
ls results/div2k_8q_pca_vs_block_dct/by_basis/
ls results/div2k_8q_pca_vs_block_dct/by_basis/qft/
ls results/div2k_8q_pca_vs_block_dct/by_basis/blocked/
cat results/div2k_8q_pca_vs_block_dct/by_basis/_baselines.json | python -c "import sys, json; d = json.load(sys.stdin); print(sorted(d.keys()))"
```

Expected:
- `by_basis/` contains 7 cell dirs (`blocked`, `entangled_qft`, `mera`, `qft`, `real_rich`, `rich`, `tebd`) plus `_baselines.json`.
- Each cell contains `metrics.json`, `env.json`, `trained_<basis>.json`, `loss_history/<basis>_loss.json`, plus PCA `*_eigenbasis.npz` files.
- `_baselines.json` contains keys `["block_dct_8", "block_fft_8", "block_pca_8", "dct", "fft", "pca"]`.

### Task B3: Cleanup `_runs/`

- [ ] **Step B3.1: Verify cellify parity, then delete `_runs/`**

Run:
```bash
# Sanity check: the cellified cells have non-empty metrics
python -c "
import json, pathlib
root = pathlib.Path('results/div2k_8q_pca_vs_block_dct/by_basis')
for d in sorted(root.iterdir()):
    if d.is_dir():
        m = json.loads((d / 'metrics.json').read_text())
        keys = list(m.keys())
        assert keys == [d.name], f'{d.name}: expected key {d.name}, got {keys}'
        print(f'{d.name}: ok')
print('parity OK')
"

# Drop _runs/
rm -rf results/div2k_8q_pca_vs_block_dct/_runs/
ls results/div2k_8q_pca_vs_block_dct/
```

Expected: parity check prints `<basis>: ok` × 7 then `parity OK`; `_runs/` is gone.

- [ ] **Step B3.2: Commit the cellified results**

Run:
```bash
git add results/div2k_8q_pca_vs_block_dct/by_basis/
git status --short results/div2k_8q_pca_vs_block_dct/
git commit -m "$(cat <<'EOF'
results(div2k-8q): training run @ generalized preset, n_train=500

7 trained bases × 6 baselines (FFT/DCT/block_FFT/block_DCT/PCA/block_PCA).
Run on two GPUs in parallel (4 unblocked + 3 blocked), then cellified
into by_basis/<basis>/. _baselines.json contains the shared classical
baselines metrics. _runs/ scratch dirs deleted after cellify parity
check.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(`trained_*.json` and `loss_history/` are gitignored per `.gitignore` — only the small text artifacts land in the commit.)

---

## Phase C — Independent rerun, figures, paper table

### Task C1: Independent baseline rerun

- [ ] **Step C1.1: Run the verifier**

Run:
```bash
python tools/independent_div2k_8q_baselines.py --gpu 0 --seed 42 --n-train 500
```

Expected: prints baseline-by-baseline progress; writes `results/div2k_8q_pca_vs_block_dct/independent_reruns/seed_default/{metrics.json, REPORT.md}`. Wall-clock minutes (no training, just baseline fits + evaluation).

- [ ] **Step C1.2: Verify**

Run:
```bash
ls results/div2k_8q_pca_vs_block_dct/independent_reruns/seed_default/
head -20 results/div2k_8q_pca_vs_block_dct/independent_reruns/seed_default/REPORT.md
```

Expected: `metrics.json` + `REPORT.md`. Report shows the 6 baselines and their PSNR table.

- [ ] **Step C1.3: Commit**

Run:
```bash
git add results/div2k_8q_pca_vs_block_dct/independent_reruns/
git commit -m "$(cat <<'EOF'
results(div2k-8q): independent baseline rerun, seed=42

Verifies the classical-baseline numbers against a fresh fit-and-eval
loop with no trained bases. Should match _baselines.json in the
cellified tree at the same seed.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task C2: Render the freq+recon grid

- [ ] **Step C2.1: Pick two visually-distinct DIV2K test images**

The test split is loaded with `load_div2k(n_train=500, n_test=50, seed=42, size=256)`. Image indices are `0..49`. To pick visually-distinct ones, dump a few thumbnails:

Run:
```bash
python -c "
import os
os.environ['CUDA_VISIBLE_DEVICES'] = '0'
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
from pdft_benchmarks.datasets import load_div2k
_, test = load_div2k(500, 50, seed=42, size=256)
fig, axes = plt.subplots(5, 10, figsize=(20, 10))
for i, ax in enumerate(axes.flat):
    ax.imshow(test[i], cmap='gray')
    ax.set_title(str(i))
    ax.axis('off')
plt.tight_layout()
plt.savefig('/tmp/div2k_test_thumbnails.png', dpi=80)
print('wrote /tmp/div2k_test_thumbnails.png')
"
```

Expected: writes a 5×10 grid thumbnail to `/tmp/div2k_test_thumbnails.png`. Inspect it (open in viewer or scp). Pick two indices `<i>,<j>` where the images are visually distinct (e.g. one high-frequency natural texture, one smoother subject).

For reproducibility, record the chosen indices here and propagate to the writeup. **Default fallback if you don't want to pick: use indices 0 and 5.** Document the choice in Step C2.3's commit message.

- [ ] **Step C2.2: Render the grid**

Run:
```bash
python tools/render_freq_recon_grid.py \
    --dataset div2k_8q --gpu 0 \
    --image-indices 0,5 \
    --keep-ratios 0.05,0.10,0.15,0.20
```

(Substitute the indices you picked.)

Expected: writes `results/div2k_8q_pca_vs_block_dct/figures/freq_recon_grid_img0.png`, `..._img0_freq.png`, `..._img5.png`, `..._img5_freq.png` (or whatever indices you used).

- [ ] **Step C2.3: Verify and commit**

Run:
```bash
ls -la results/div2k_8q_pca_vs_block_dct/figures/freq_recon_grid_*
git add results/div2k_8q_pca_vs_block_dct/figures/
git commit -m "$(cat <<'EOF'
figures(div2k-8q): freq+recon grids for test images 0 and 5

Two visually-distinct DIV2K test images at four keep ratios (0.05,
0.10, 0.15, 0.20) under each method. Renders one row per method, two
columns (frequency spectrum + reconstruction) per image. Image
indices recorded here for reproducibility.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(Update the indices `0 and 5` in the commit message to match what was actually rendered.)

### Task C3: Render the PCA basis visualization

- [ ] **Step C3.1: Render**

Run:
```bash
python tools/render_pca_basis_visualization.py \
    --dataset div2k_8q --gpu 0 \
    --n-train 500 --top-k 16
```

Expected: writes `results/div2k_8q_pca_vs_block_dct/figures/pca_basis.png`.

- [ ] **Step C3.2: Verify and commit**

Run:
```bash
ls -la results/div2k_8q_pca_vs_block_dct/figures/pca_basis.png
git add results/div2k_8q_pca_vs_block_dct/figures/pca_basis.png
git commit -m "$(cat <<'EOF'
figures(div2k-8q): PCA basis visualization

Top-16 eigenvectors of global PCA (256×256) and block PCA (8×8) fit
on the n_train=500 DIV2K-8q train split, plus DCT for reference. Same
3-panel layout as the QuickDraw equivalent.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task C4: Copy AR(1) figure from QuickDraw

- [ ] **Step C4.1: Copy**

Run:
```bash
cp results/quickdraw_pca_vs_block_dct/figures/ar1_examples.png \
   results/div2k_8q_pca_vs_block_dct/figures/ar1_examples.png
```

Expected: file exists at the new location.

- [ ] **Step C4.2: Commit**

Run:
```bash
git add results/div2k_8q_pca_vs_block_dct/figures/ar1_examples.png
git commit -m "$(cat <<'EOF'
figures(div2k-8q): copy AR(1) examples from QuickDraw

The AR(1) figure is dataset-agnostic and used in both writeups. Copy
(not symlink) so each writeup tree is self-contained per the reorg
spec's principle.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task C5: Render the paper table

- [ ] **Step C5.1: Render**

Run:
```bash
python tools/render_div2k_paper_table.py \
    --by-basis results/div2k_8q_pca_vs_block_dct/by_basis \
    --out     results/div2k_8q_pca_vs_block_dct/tables/published_8q_div2k.tex
```

Expected: writes the .tex file. Output prints `[paper-table] wrote <path> (<N> bytes)`.

- [ ] **Step C5.2: Sanity-check the table contents**

Run:
```bash
cat results/div2k_8q_pca_vs_block_dct/tables/published_8q_div2k.tex
```

Expected: a `\begin{tabular}` block with rows for FFT, DCT, Block-FFT 8, Block-DCT 8, PCA, Block-PCA 8, QFT, Entangled QFT, TEBD, MERA, Blocked, Rich, Real Rich. PSNR values populated (not `—`) for every cell.

If any row shows `—`, that means the renderer couldn't find that key in either `_baselines.json` or `<basis>/metrics.json`. Inspect and fix before committing.

- [ ] **Step C5.3: Commit**

Run:
```bash
git add results/div2k_8q_pca_vs_block_dct/tables/published_8q_div2k.tex
git commit -m "$(cat <<'EOF'
tables(div2k-8q): paper table generated from cellified results

Same column-format as published_8q_quickdraw_v2.tex: rows grouped
classical-fixed / classical-fitted (PCA) / learned-unblocked /
learned-blocked, columns for keep ratios 0.05/0.10/0.15/0.20.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase D — Writeup

### Task D1: Clone the QuickDraw writeup as the DIV2K starting point

- [ ] **Step D1.1: Copy the typ source**

Run:
```bash
cp results/quickdraw_pca_vs_block_dct/writeup.typ \
   results/div2k_8q_pca_vs_block_dct/writeup.typ
```

Expected: file copied.

- [ ] **Step D1.2: Update title and any QuickDraw-specific framing**

Open `results/div2k_8q_pca_vs_block_dct/writeup.typ`. Apply the following retargeting edits:

A) Title line — find the `#title` or top-level heading and change "QuickDraw" → "DIV2K-8q natural images" (or similar).

B) Abstract / intro — replace narrative about "line-drawing imagery" / "32×32 QuickDraw" with "natural images" / "256×256 DIV2K".

C) §3 (Fits) — the math is dataset-agnostic; no change. Update any concrete dimensionality numbers: `d = 1024` (32×32) → `d = 65536` (256×256), `N = 500` stays the same.

D) §4 (Results) — replace `\input{tables/published_8q_quickdraw_v2.tex}` with `\input{tables/published_8q_div2k.tex}` (the actual reference may use Typst syntax instead of LaTeX-style; preserve whatever syntax the typ uses).

E) §5 (Reconstructions) — image references like `figures/freq_recon_grid_img0.png` already use relative paths; they resolve against the new figures dir. Update narrative captions to reflect the chosen DIV2K images (e.g. "image #0 (cat)" → describe what the actual image is).

F) §6 (Matching) — paper-figure crosswalk. Update references that name QuickDraw-specific paper figures.

G) Block-size mismatch note — add a sentence acknowledging that classical block_dct_8 uses 8×8 blocks while trained block bases (blocked / rich / real_rich) at m=8 use 16×16 blocks (per the `_blocked` factory). Mention this is the same asymmetry as QuickDraw (4×4 trained vs 8×8 classical), so the comparison framing carries over.

H) MERA paragraph — note that MERA actually runs at m=n=8 (m+n=16 = 2^4) unlike the QuickDraw experiment where it's silently skipped. The MERA row in the table is meaningful here.

- [ ] **Step D1.3: Compile the writeup**

Run:
```bash
cd results/div2k_8q_pca_vs_block_dct
typst compile writeup.typ writeup.pdf
cd /home/claude-user/pdft-benchmarks
ls -la results/div2k_8q_pca_vs_block_dct/writeup.pdf
```

Expected: typst exits 0; PDF written.

If typst errors (e.g. missing figure, malformed reference): fix the offending typ section, recompile.

- [ ] **Step D1.4: Sanity-check the PDF**

Open the PDF in a viewer. Verify:
- Title says DIV2K (not QuickDraw).
- Table populated with real numbers, not placeholders.
- Figures rendered.
- All cross-references resolve (no `??` markers).

- [ ] **Step D1.5: Commit**

Run:
```bash
git add results/div2k_8q_pca_vs_block_dct/writeup.typ results/div2k_8q_pca_vs_block_dct/writeup.pdf
git commit -m "$(cat <<'EOF'
writeup(div2k-8q): clone QuickDraw writeup, retarget to natural images

Same six-section structure (intro / fits / results / reconstructions
/ matching / refs). Updated narrative paragraphs to talk about
natural images (256×256 grayscale DIV2K) instead of line drawings.
References the DIV2K-8q paper table and freq+recon figures rendered
in Phase C. Notes the same block-size asymmetry (classical 8×8 vs
trained 16×16 at m=8) as QuickDraw, and the new MERA row that
silently skipped in QuickDraw but actually runs here.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase E — README delta + push + PR

### Task E1: Update top-level `README.md` to flip DIV2K-8q from placeholder to implemented

- [ ] **Step E1.1: Read the current README**

Run: `cat README.md`

Note the "Experiments" section that lists DIV2K-8q as a placeholder.

- [ ] **Step E1.2: Replace the DIV2K-8q line**

Edit `README.md`. Find:
```markdown
2. **DIV2K-8q** (m=n=8, 256×256) — placeholder. The paper needs a
   DIV2K-8q analog of the QuickDraw experiment, including MERA on the
   unblocked variant. See `results/div2k_8q_pca_vs_block_dct/README.md`
   and the follow-on spec.
```

Replace with:
```markdown
2. **DIV2K-8q** (m=n=8, 256×256) — implemented. Results, figures,
   table, and writeup live in `results/div2k_8q_pca_vs_block_dct/`.
   Includes MERA on the unblocked variant (m+n=16 is a power of 2,
   unlike QuickDraw at m+n=10).
```

Find the "Running" section. Add a DIV2K block immediately after the QuickDraw block:
```bash
# Train DIV2K-8q across two GPUs in parallel
python experiments/div2k_8q_pca_vs_block_dct.py \
    --gpu 0 --bases qft,entangled_qft,tebd,mera \
    --out results/div2k_8q_pca_vs_block_dct/_runs/unblocked
python experiments/div2k_8q_pca_vs_block_dct.py \
    --gpu 1 --bases blocked,rich,real_rich \
    --out results/div2k_8q_pca_vs_block_dct/_runs/blocked
# After both finish:
python tools/cellify_run.py --in <each _runs/X> --out results/div2k_8q_pca_vs_block_dct/by_basis
```

- [ ] **Step E1.3: Commit**

Run:
```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs(README): DIV2K-8q is now implemented, not a placeholder

Flips the "Experiments" #2 line from placeholder to implemented and
adds the two-GPU run sequence to "Running".

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task E2: Update `results/div2k_8q_pca_vs_block_dct/README.md`

- [ ] **Step E2.1: Replace the placeholder README**

Replace the entire contents of `results/div2k_8q_pca_vs_block_dct/README.md` with:

```markdown
# DIV2K-8q PCA-vs-block-DCT

m=n=8, 256×256 grayscale natural images. Seven trainable parametric
bases (qft, entangled_qft, tebd, mera, blocked, rich, real_rich)
benchmarked against six classical baselines (FFT, DCT, block_FFT_8,
block_DCT_8, PCA, block_PCA_8) at keep ratios 0.05 / 0.10 / 0.15 / 0.20.

## Layout

- `by_basis/<basis>/` — one cell per trained basis: `metrics.json`,
  `env.json`, `trained_<basis>.json`, `loss_history/`, plus PCA
  eigenbasis files copied for self-containment.
- `by_basis/_baselines.json` — classical baseline metrics shared
  across cells (output of `tools/cellify_run.py`).
- `independent_reruns/seed_default/` — independent verification of
  the classical baselines via `tools/independent_div2k_8q_baselines.py`.
- `figures/` — `ar1_examples.png` (copied from QuickDraw),
  `freq_recon_grid_img*.png`, `pca_basis.png`.
- `tables/published_8q_div2k.tex` — paper table.
- `writeup.typ` + `writeup.pdf` — paper writeup section.

## Reproducing

```bash
# Two-GPU training (4 unblocked + 3 blocked)
python experiments/div2k_8q_pca_vs_block_dct.py \
    --gpu 0 --bases qft,entangled_qft,tebd,mera \
    --out results/div2k_8q_pca_vs_block_dct/_runs/unblocked
python experiments/div2k_8q_pca_vs_block_dct.py \
    --gpu 1 --bases blocked,rich,real_rich \
    --out results/div2k_8q_pca_vs_block_dct/_runs/blocked

# Cellify
python tools/cellify_run.py \
    --in  results/div2k_8q_pca_vs_block_dct/_runs/unblocked \
    --out results/div2k_8q_pca_vs_block_dct/by_basis
python tools/cellify_run.py \
    --in  results/div2k_8q_pca_vs_block_dct/_runs/blocked \
    --out results/div2k_8q_pca_vs_block_dct/by_basis
rm -rf results/div2k_8q_pca_vs_block_dct/_runs/

# Verify, render, table, writeup
python tools/independent_div2k_8q_baselines.py --gpu 0 --seed 42 --n-train 500
python tools/render_freq_recon_grid.py --dataset div2k_8q --gpu 0
python tools/render_pca_basis_visualization.py --dataset div2k_8q --gpu 0
cp results/quickdraw_pca_vs_block_dct/figures/ar1_examples.png figures/
python tools/render_div2k_paper_table.py
typst compile results/div2k_8q_pca_vs_block_dct/writeup.typ
```

Block-size note: the trainable block bases (blocked / rich / real_rich)
use 16×16 blocks at m=8 via the symmetric `_blocked` split
(inner_m=4, block_log_m=4). The classical `block_dct_8` and
`block_fft_8` baselines use 8×8 blocks. The trained-vs-classical
block-size asymmetry mirrors the QuickDraw experiment (4×4 trained
vs 8×8 classical at m=5).
```

- [ ] **Step E2.2: Commit**

Run:
```bash
git add results/div2k_8q_pca_vs_block_dct/README.md
git commit -m "$(cat <<'EOF'
docs(div2k-8q): real README replacing the placeholder

Documents the layout, the two-GPU run sequence, and the
trained-vs-classical block-size asymmetry.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Task E3: Push and open PR

- [ ] **Step E3.1: Final pre-push verification**

Run:
```bash
git log --oneline main..HEAD
git status --short
```

Expected: a list of commits showing Task A1–A6 + B2/B3/C1–C5 + D1 + E1/E2. Status clean.

- [ ] **Step E3.2: Push**

Run:
```bash
git push -u origin feat/div2k-8q-pca-vs-block-dct
```

Expected: push succeeds.

- [ ] **Step E3.3: Open PR**

Run:
```bash
gh pr create --base main --title "DIV2K-8q PCA-vs-block-DCT experiment" --body "$(cat <<'EOF'
## Summary

Implements the DIV2K-8q analog of the QuickDraw PCA-vs-block-DCT experiment (m=n=8, 256×256 natural images). Mirrors the QuickDraw layout under `results/div2k_8q_pca_vs_block_dct/`. Adds MERA on the unblocked variant (m+n=16 = 2^4, unlike QuickDraw at m+n=10 where MERA is silently skipped).

## What's in this PR

- `experiments/div2k_8q_pca_vs_block_dct.py` — real entry point replacing the placeholder. Sets `CUDA_VISIBLE_DEVICES` before JAX import; accepts `--bases` for split-across-GPU runs; baselines list includes PCA + block-PCA.
- `tools/cellify_run.py` — flat `run_experiment` output → `by_basis/<basis>/` tree (replaces deleted `extract_canonical_cells.py`). Also writes `_baselines.json`. Pure-Python (no GPU).
- `tools/independent_div2k_8q_baselines.py` — DIV2K analog of `independent_quickdraw_baselines.py`.
- `tools/render_div2k_paper_table.py` — DIV2K-specific paper table renderer.
- `tools/render_freq_recon_grid.py`, `tools/render_pca_basis_visualization.py` — extended with `--dataset {quickdraw,div2k_8q}` flag.
- `results/div2k_8q_pca_vs_block_dct/` — populated `by_basis/`, `independent_reruns/seed_default/`, `figures/`, `tables/`, `writeup.{typ,pdf}`.
- `README.md` updates.

## Test plan

- [ ] `typst compile results/div2k_8q_pca_vs_block_dct/writeup.typ` succeeds.
- [ ] `python tools/render_freq_recon_grid.py --dataset div2k_8q --gpu 0` regenerates the existing PNGs (deterministic at seed 42).
- [ ] `python tools/independent_div2k_8q_baselines.py --gpu 0 --seed 42 --n-train 500` produces baseline numbers within ±0.05 dB of `_baselines.json`.
- [ ] `pytest tests/ --no-cov` passes.
- [ ] `--help` of new and modified tools renders cleanly.

## Notes

- Block bases at m=8 use 16×16 blocks (`_blocked` symmetric split: inner_m=4, block_log_m=4). Classical `block_dct_8` uses 8×8 blocks. Trained-vs-classical block-size asymmetry is documented in the writeup; same pattern as QuickDraw.
- `tools/render_paper_table.py` (the older general renderer) still has the directory-walk TODO from the reorg; this PR adds a DIV2K-specific renderer rather than fixing the general one.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL printed.

---

## Self-Review

**Spec coverage:**
- Goal — Tasks A1, B1, C1–C5, D1, E1, E2 cover the deliverable.
- Components (spec §"Components") — A1 (entry point), A2+A4.3 (cellify), A3 (independent rerun), A4 (paper table), A5 (freq+recon), A6 (PCA viz). AR(1) figure is C4 (copy). Writeup is D1. README is E1+E2.
- Run sequence — B1 maps to spec §"Run sequence Stage 1", B2+B3 to Stage 2, C1–C5 + D1 to Stage 3.
- Risks — addressed: GPU isolation contract enforced in A1; cellify schema verified empirically; block geometry note in writeup (D1.2 step G); resume story documented in B1.3; cleanup in B3.1.
- Non-goals — `tools/render_paper_table.py` general fix and theory/ restoration both excluded, matching spec.

**Type / signature consistency:**
- `--dataset {quickdraw,div2k_8q}` consistent across A5 (render_freq_recon_grid) and A6 (render_pca_basis_visualization).
- `_baselines.json` as the canonical baseline source: emitted by A4.3, consumed by A4.6 (paper table renderer). Names match.
- `--gpu N` → `CUDA_VISIBLE_DEVICES=str(N)` pattern: same in A1 (entry point), A3 (independent rerun). Cellify (A2) deliberately doesn't import pdft_benchmarks and is verified by grep in Step A2.3.
- `_runs/<group>/` paths consistent across B1, B2, B3.

**Placeholder scan:**
- No "TBD"/"TODO" in plan steps. The image-index choice in C2.1 documents a fallback (0 and 5) so the step is concrete; the engineer can override if they pick differently.
- Every code edit shows `old_string` → `new_string` (or full replacement contents).
- Every command has an expected outcome.
- Step D1.2's writeup-narrative edits are described abstractly per-section because the typ source is hundreds of lines and per-line edits would bloat this plan; engineer applies judgment within the listed retargeting points (A–H). This is a deliberate scope decision — the writeup is content authoring, not mechanical patching.
